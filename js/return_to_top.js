let fetch_wrapper = document.getElementsByClassName("wrapper");
let total_wrappers = fetch_wrapper.length;

if(total_wrappers > 2) {

	document.write("<a href=\"#top\" id=\"return_btn\">&uarr;</a>");
}